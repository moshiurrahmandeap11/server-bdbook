enum UserRole {
  user
  admin
}

enum Gender {
  male
  female
  other
}

model User {
  id String @id @default(uuid())
  email String @unique
  username String? @unique
  password String
  fullName String
  role UserRole @default(user)
  gender Gender?
